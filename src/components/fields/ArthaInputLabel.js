import { InputLabel } from "@mui/material";

import { styled } from "@mui/material/styles";
const ArthaInputLabel = styled(InputLabel)({
  "& .MuiInputBase-root": {
    fontSize: "13px", // Adjust the font size as needed
  },
});

export default ArthaInputLabel;
